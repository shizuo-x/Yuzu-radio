# version: '3.8'
services:
  yuzu:
    build: .
    container_name: yuzu-discord-bot
    restart: unless-stopped
    env_file:
      - .env
    # Optional volume for logs if needed
    # volumes:
    #   - ./logs:/app/logs